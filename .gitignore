# Ignorar TODO por defecto
/*

# Permitir explícitamente la carpeta public y su contenido
!/public/
!/public/**

# Permitir archivos de configuración esencial en la raíz
!/.gitignore
!/README.md
!/package.json
!/next.config.ts
!/tsconfig.json

# Pero ignorar archivos pesados dentro de public (por si acaso)
public/**/*.psd
public/**/*.zip
public/**/*.pcap
